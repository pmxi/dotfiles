-- require("config.lazy")

-- vim.api.nvim_create_autocmd('FileType', {
--   pattern = 'python',
--   callback = function(args)
--     vim.lsp.start({
--       name = 'pyright',
--       cmd = {'pyright-langserver', '--stdio'},
--       root_dir = vim.fs.find({'.git', 'setup.py', 'pyproject.toml'}, { upward = true })[1] or vim.fn.getcwd()
--     })
--   end,
-- })


vim.g.mapleader = " "
vim.keymap.set("n", "<leader>pv", vim.cmd.Ex)
